//Excersise in C printing lines

#include<stdio.h>
int main(void)
{
  printf("In C, lowercase letters are significate\n", );
  printf("main() is where the program execution begins\n", );
  printf("Opening and closing braces enclose program statmnts in routine\n", );
  printf("All program statments must be terminated by a semicolon\n", );
  
  return 0;

}
